# Obsidian Vault — Стартове сховище знань

Це стартове сховище знань для Obsidian, структуроване для швидкого захоплення та системної роботи з нотатками: **Inbox → Projects/Areas/Resources → Archive**.

## Швидкий старт

1. **Відкрийте папку в Obsidian:**
   - Запустіть Obsidian
   - Натисніть `Open folder as vault`
   - Виберіть папку `/Users/anymacstore/ObsidianVault`

2. **Встановіть необхідні плагіни:**
   - Перейдіть в `Settings` → `Community plugins`
   - Увімкніть: `Daily Notes` (або `Periodic Notes`)
   - Встановіть `Templater` (опціонально)

3. **Налаштуйте шаблони:**
   - Для **Daily Notes**: `Settings` → `Daily Notes` → Set template folder path: `Templates`
   - Для **Meeting/Note**: використовуйте команду `Templater: Create new note from template`

## Структура сховища

```text
ObsidianVault/
├── MOC - Home.md
├── Inbox.md
├── README.md
├── STRUCTURE.md
├── Daily/
├── Projects/
│   ├── Example Project.md
│   └── README.md
├── Areas/
│   └── README.md
├── Resources/
│   └── README.md
├── Archive/
└── Templates/
    ├── Daily.md
    ├── Meeting.md
    └── Note.md
```

## Робочий процес

### Щоденне використання

1. **Захват:** Записуйте швидкі думки в `Inbox.md`.
2. **Щоденна нотатка:** Створіть щоденну нотатку з шаблону (`Templates/Daily.md`).
3. **Обробка Inbox:** Щодня або раз на тиждень переглядайте та переміщуйте нотатки у відповідні місця.

### Щотижневий огляд

- Очищення Inbox
- Перегляд важливих нотаток
- Оновлення статусу проектів

### Щомісячний огляд

- Архівування завершених проєктів
- Оновлення зон відповідальності (Areas)

## Поради та трюки

- Використовуйте `[[...]]` для внутрішніх посилань, наприклад:

```text
[[MOC - Home]]
[[Daily/2025-12-09]]
```

- Теги для фільтрації:

```text
tags: #daily #important #work
```

## Git & GitHub

Якщо ви хочете зберігати сховище в GitHub (рекомендовано для бекапу і версіювання):

```bash
git remote add origin git@github.com:YOUR_USERNAME/REPO_NAME.git
# або HTTPS
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

Або створіть репозиторій і запуште за допомогою GitHub CLI:

```bash
gh repo create REPO_NAME --public --source=. --remote=origin --push
```

У сховищі можуть бути налаштовані GitHub Actions для markdown lint і бекапів:

- `.github/workflows/markdownlint.yml`
- `.github/workflows/weekly-backup.yml`

## Налаштування Templater (опціонально)

1. `Settings` → `Templater` → Set "Template folder location": `Templates`
2. Приклади синтаксису:

```text
<% tp.date.now("YYYY-MM-DD") %>
<% tp.file.title %>
```

## Використання плагіна Journals (рекомендовано для щоденних нотаток)

Плагін **Journals** дозволяє легко керувати щоденними/тижневими/місцевими журналами в окремій папці і автоматично створювати нотатки за шаблоном.

### Налаштування Journals

1. Встановіть плагін: `Settings` → `Community plugins` → пошук `Journals` → `Install`
2. Увімкніть плагін: `Settings` → `Community plugins` → `Journals` → Enable
3. Налаштуйте плагін:
   - **Journals folder:** `Daily`
   - **Template file location:** `Templates/Journals/Daily.md`
   - **Date format:** `YYYY-MM-DD` (або інший за бажанням)
   - **Template folder:** `Templates/Journals`

### Важливо: Синтаксис шаблонів Journals

Шаблони Journals використовують **інший синтаксис**, ніж Templater:

| Потребу | Journals синтаксис | Приклад |
|---------|-------------------|---------|
| Поточна дата | `{{date}}` | В frontmatter: `date: {{date}}` |
| Назва файлу | `{{title}}` | У заголовку: `# {{title}}` |

⚠️ **НЕ використовуйте** синтаксис Templater (`<% ... %>`) у frontmatter, інакше Journals не зможе створювати нотатки (помилка "invalid reference date format").

### Доступні шаблони

- `Templates/Journals/Daily.md` — щоденна нотатка
- `Templates/Journals/Weekly.md` — щотижневий огляд
- `Templates/Journals/Meeting.md` — протокол зустрічі

### Переваги Journals

- Централізоване зберігання журналів у заданій папці
- Множинні журнали для різних контекстів (робота, особисте, проекти)
- Навігація між період ами (попередня/наступна дата)
- Автоматичне заповнення дат за форматом шаблону
- Оформлення з іконками, кольорами, групуванням


## Зберігання вкладень і файлів (гібридна стратегія)

Сховище використовує **гібридний підхід** до зберігання файлів та медіа:

### Централізовані ресурси (`Files/`)

Зберігайте в `Files/` загальні, часто повторювані файли:

- Зображення, іконки, логотипи (що використовуються в кількох нотатках)
- Шаблони, документи, PDF (спільне користування)
- Інші невеликі ресурси

Підпапки для організації: `Files/images/`, `Files/docs/`, `Files/templates/` тощо.

### Per-note/per-project папки

Для нотаток або проектів з багатьма вкладеннями створюйте папку поруч з нотаткою:

- Наприклад: `Projects/Example Project/attachments/` або `Daily/2025-12-09/assets/`
- Переваги: контекстна організація, легко архівувати/видаляти проект разом з файлами

### Налаштування Obsidian (`Settings → Files & Links`)

**Для централізованого підходу (Inbox, Daily):**

- **Default location for new attachments**: `In the vault root` → вкажіть `Files/`

**Для per-project вкладень:**

- **Default location for new attachments**: `In the same folder as current file`
- Тоді файли автоматично зберігатимуться в папці поруч з нотаткою

Вибір залежить від контексту: для щоденних нотаток обирайте `Files/`, для проектів — `In the same folder`.

### Великі файли та Git LFS

Якщо ви зберігаєте великі бінарні файли (відео, архіви, дизайн-файли), розгляньте Git LFS:

1. Установіть Git LFS: [https://git-lfs.com/](https://git-lfs.com/)
2. Інсталюйте в репозиторії:

```bash
git lfs install
```

3. Типи файлів вже налаштовані в `.gitattributes` (mp4, mov, zip, pdf, psd тощо)
4. Після першого push великих файлів вони будуть відстежуватися автоматично


**Без Git LFS:** для значних бінарних файлів додайте їх в `.gitignore`, щоб не закомічувати, або використовуйте зовнішнє сховище (хмара, Google Drive тощо).

### Нейминг файлів

- Уникайте пробілів і спецсимволів: використовуйте дефіси замість пробілів (`my-image.png` замість `my image.png`)
- Додавайте префікси для унікальності в `Files/`: `2025-12-09-meeting-notes.pdf`, `logo-v2-dark.png`


---

**Вдалого ведення сховища!** 📚
